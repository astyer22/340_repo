<!-- /inventory/add-vehicle.ejs -->

<!-- Content Heading -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.rediredct('/')
    } %>
<%- messages() %> <!-- Flash messages -->

<% if (errors) { %>
    <ul class="notice">
    <% errors.array().forEach(error => { %>
    <li><%= error.msg %></li>
<%  }) %>
    </ul>
<% } %>
